<?xml version="1.0" encoding="utf-8"?>
<resources>
<!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme attributes here -->
        <item name="android:windowFullscreen">true</item> <!-- This makes the app full-screen -->
        <item name="android:windowContentOverlay">@null</item> <!-- Remove any content overlay -->
        <!-- Add other theme attributes as needed -->
    </style>
</resources>
